# Copy this file to .env before running locally
# cp .env.example .env

# RPC endpoint for local or remote node
RPC_URL=http://localhost:8545

# Network name
NETWORK=development

# Optional API key (if required)
API_KEY=

# Logging level
LOG_LEVEL=info
